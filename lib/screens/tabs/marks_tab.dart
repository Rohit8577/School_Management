import 'package:flutter/material.dart';

class MarksTab extends StatelessWidget {
  const MarksTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Marks Tab Placeholder'),
    );
  }
}
